#include <iostream>
#include <vector>
#include "myFunctorClass.h"
#include "Message.h"

int main (int argc, char** argv)

{
	std::vector<Message> messages;
	
	std::string strs[4] = ("so1", "so2", "so1", "spo2");
	
	for (int i=0, i<4; i++)
		messages.push(str[i]);

	return 0;
}
